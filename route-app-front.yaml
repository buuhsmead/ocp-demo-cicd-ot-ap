apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: app-front
  name: app-front
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: app-front
    weight: 100
  wildcardPolicy: None
